# Local secrets for CLI tools (copy to .env — never commit .env).
#
# Shipped app / flutter run config uses env.dev.json instead:
#   copy env.dev.json.example → env.dev.json
#   run: .\scripts\run_dublin.ps1
#
# POI seeding uses OpenStreetMap/Overpass — no API key required.
# (Legacy HERE_API_KEY is no longer used.)
