import 'package:flutter/material.dart';

/// Formerly explained Just Landed / Grand / Sound host badges.
/// Phase 2: removed — no trust-tier help dialog.
class TrustBadgesHelpButton extends StatelessWidget {
  const TrustBadgesHelpButton({super.key});

  static Future<void> showExplainer(BuildContext context) async {}

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
